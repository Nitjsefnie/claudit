<!--
The PR Report Contract, rendered as a form.

  required     always present; when the answer is "none", say so explicitly
  conditional  always CHECKED, but dropped entirely — header included — when
               the condition is not met
  optional     dropped entirely when it does not apply

Section order is fixed. No new sections. Delete these comments before posting.
-->

## Summary
<!-- required: 1-2 sentences — what changed and why. -->

## Related Issues and Pull Requests
<!-- conditional: the issue this PR resolves, plus related work. Closing
     keywords ONLY here. Drop the whole section if it resolves and relates to
     nothing. GitHub auto-closes only on merge into the DEFAULT branch. -->

Fixes #

## Changes
<!-- required: bullet list of concrete changes — files, modules, behavior. -->

-

## Testing
<!-- required: what was actually run or checked. If nothing was tested, say so
     and why. -->

## Bugs Discovered
<!-- conditional: pointer list of ALREADY-FILED issues — number plus the
     issue's literal title, copied exactly. No paraphrase, no description; the
     report lives in the issue. Drop the section if none were found. -->

-

## Breaking Changes
<!-- optional -->

## Follow-ups / Known Limitations
<!-- optional: caveats about our own fix go here; this never waives filing a
     durable defect as its own issue. -->

## Dependencies
<!-- optional -->

## Footer
<!-- required, last line. One entry per model, roles merged — never repeat a
     model name. "Generated by", never "via" or "with". -->

Generated by <model> (<role>, <role>)
